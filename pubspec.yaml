name: my_crypto_safe
description: Application Flutter de démarrage pour My Crypto Safe
publish_to: 'none'

version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  flutter_secure_storage: ^9.2.2
  bip39: ^1.0.6
  web3dart: ^2.7.3
  http: ^1.2.2
  qr_flutter: ^4.1.0
  local_auth: ^2.3.0
  shared_preferences: ^2.3.2
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
